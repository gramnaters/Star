name: Build

on:
  push:
    branches: [master]
    paths:
      - 'patches/**'
      - 'extensions/**'
      - 'gradle/**'
      - '.github/workflows/build.yml'
    tags:
      - 'v*'
  workflow_dispatch:

env:
  VERSION: ${{ github.ref_name }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Build patches
        run: ./gradlew :patches:build :patches:generatePatchesList --stacktrace
        env:
          GITHUB_TOKEN: ${{ secrets.GPR_KEY }}
          GITHUB_ACTOR: ${{ github.actor }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: star-patches
          path: patches/build/libs/*.mpp
          retention-days: 7

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: patches/build/libs/*.mpp
          tag_name: ${{ github.ref_name }}
          name: "Star ${{ github.ref_name }}"
          body: |
            Star patches for JioHotstar ${{ github.ref_name }}

            ## Changes
            See commits for details.
          prerelease: false
